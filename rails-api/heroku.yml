setup:
  config:
    RAILS_ENV: staging
    RACK_ENV: staging
build:
  docker:
    web: Dockerfile
  config:
    RAILS_ENV: staging
    RACK_ENV: staging
run:
  web: bundle exec puma -C config/puma-staging.rb
  worker:
    command:
      - bundle exec sidekiq
    image: web